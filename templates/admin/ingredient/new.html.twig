{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouvel ingrédient{% endblock %}

{% block content %}
    <div class="container-fluid">
        <header class="mb-4">
            <div class="container py-3">
                <div class="row align-items-center">
                    <div class="col-6">
                        <h1>Ajouter un nouvel ingrédient</h1>
                    </div>
                 
                </div>
            </div>
        </header>

        <div class="container">
            <div class="section">
                <form action="{{ path('admin_ingredients_new') }}" method="post"> {{ form_widget(form) }} <button type="submit" class="btn btn-primary">Enregistrer</button>
                    <a href="{{ path('admin_ingredients_index') }}" class="btn btn-danger">Annuler</a>
                </form> </div>
        </div>

{% endblock %}
