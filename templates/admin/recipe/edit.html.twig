{% extends 'base.html.twig' %}

{% block title %}Modifier la recette{% endblock %}

{% block body %}
    <h1>Modifier la recette</h1>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning">{{ button_label|default('Sauvegarde') }}</button>
{{ form_end(form) }}
{% endblock %}
