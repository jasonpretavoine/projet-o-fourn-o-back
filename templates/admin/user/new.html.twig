{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouvel utilisateur
{% endblock %}

{% block content %}
	<div class="container">


		<div class="main">
			<div class="section">
				<h1>Ajouter un nouvel utilisateur</h1>

				{{ form_start(form) }}
				{{ form_widget(form) }}
				<button type="submit" class="btn btn-sm col-blue text-white fw-bold">Enregistrer</button>
				<a href="{{ path('admin_users') }}" class="btn btn-sm col-orange text-white fw-bold">Annuler</a>
				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}
